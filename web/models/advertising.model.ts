export interface Campaign {
    id: number,
    name: string
}

export interface DataSource {
    id: number,
    name: string
}

export interface DataSample {
    sampleDate: number,
    clicks: number,
    impressions: number
}
