export interface Campaign {
    id: number,
    name: string
}

export interface DataSource {
    id: number,
    name: string
}

export interface DateSample {
    sampleDate: string,
    clicks: number,
    impressions: number
}
